// Re-exports corpus-specific types from the pipeline types.
// Add corpus-domain extensions here as the slice matures.
export type { EvidenceMessage, Cluster, MiningCorpus, AnalysisRun }
  from '../../types/pipeline';
